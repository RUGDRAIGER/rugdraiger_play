{
  "name": "rugdraiger-play-desktop-macos",
  "version": "1.0.0",
  "private": true,
  "description": "Rugdraiger Play - escritorio macOS (misma UI que la web/APK)",
  "main": "main.js",
  "scripts": {
    "build:web": "cd ../../web && (npm ci || npm install) && VITE_CAPACITOR=true npm run build",
    "pack:mac": "npm run build:web && npm run pack:mac:only",
    "pack:mac:only": "electron-packager . \"Rugdraiger Play\" --platform=darwin --arch=${ELECTRON_ARCH:-arm64} --out=../_packager-out --overwrite --ignore=\"node_modules|package-lock.json|\\.git|assets/app.iconset|assets/app-icon.png\" --extra-resource=../../web/dist"
  },
  "devDependencies": {
    "@electron/packager": "^18.4.4",
    "electron": "^33.2.1"
  }
}
