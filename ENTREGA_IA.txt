================================================================================
RUGDRAIGER PLAY — DOCUMENTO DE ENTREGA PARA OTRA IA
================================================================================
Repositorio: https://github.com/RUGDRAIGER/rugdraiger_play
  Fecha referencia: mayo 2026
  Última actualización documentada: 23 mayo 2026 — carátulas APK + deploy gh-pages

Este archivo describe SOLO:
  A) La aplicación WEB (referencia de diseño y funcionalidad)
  B) La aplicación ANDROID NATIVA Flutter (última versión publicada)

NO usar Capacitor/WebView como app principal. El APK publicado es Flutter nativo.


================================================================================
A) PÁGINA WEB — Rugdraiger Play
================================================================================

--- URL y despliegue ---

  Producción: https://rugdraiger.github.io/rugdraiger_play/
  Base path build: /rugdraiger_play/
  Carpeta fuente: web/
  Build: cd web && npm run build:pages
  Deploy: scripts/deploy-pwa.sh → rama gh-pages
  APK descargable desde web: /rugdraiger_play/apk/rugdraiger-play.apk

--- Stack tecnológico ---

  React 18 + TypeScript + Vite 6
  Estado: Zustand (4 stores)
  Persistencia: Dexie (IndexedDB) — DB "RugdraigerPlayerDB" v3
  Metadatos audio: jsmediatags (ID3/APIC en cliente)
  Reproducción: HTMLAudioElement + Web Audio API (ecualizador real)
  PWA: vite-plugin-pwa (service worker, manifest)
  Capacitor en web/ existe pero NO es la app Android oficial

--- Identidad visual (CSS variables en web/src/styles/globals.css) ---

  --bg-primary:     #0A0A0A
  --bg-secondary:   #141414
  --bg-surface:     #1C1C1E
  --bg-surface2:    #2C2C2E
  --bg-surface3:    #3A3A3C
  --accent:         #FF2020
  --accent-dim:     #CC1A1A
  --text-primary:   #FFFFFF
  --text-secondary: #8E8E93
  --text-tertiary:  #48484A
  --border:         rgba(255,255,255,0.08)
  --mini-player-height: 76px
  --bottom-nav-height:  56px
  Tipografía: system-ui / SF Pro / Segoe UI

--- Arquitectura UI (web/src/App.tsx) ---

  Navegación por estado local (ViewName), sin React Router.
  Desktop (≥768px): Sidebar lateral 220px + contenido + MiniPlayer abajo
  Móvil (<768px):   Contenido + MiniPlayer + BottomNav fijo + drawer "Más"

  Vistas (ViewName en web/src/types/index.ts):
    home, library, songs, albums, artists, playlists, equalizer, search
    (playlist-detail existe en tipos; lógica dentro de PlaylistsView)

  Bottom nav móvil (5 tabs + Más):
    Inicio | Canciones | Playlists | Ecualizador | Buscar | Más
  Drawer "Más" incluye las 8 vistas completas + link Download APK (solo web)

--- Pantallas y archivos ---

  HomeView          web/src/components/home/HomeView.tsx
    - Logo, stats (canciones/álbumes)
    - Escanear disco/carpeta (File System Access API o input webkitdirectory)
    - Agregar archivos sueltos
    - Botón descargar APK Android
    - Recientes, preview álbumes

  LibraryView       web/src/components/library/LibraryView.tsx
    - Hub con 4 tarjetas: Canciones, Álbumes, Artistas, Playlists

  SongsView         web/src/components/library/SongsView.tsx
    - Tabla: #, título, álbum, artista, formato, duración, menú ⋮
    - Búsqueda, orden por columnas, Reproducir todo
    - SongActionsMenu por fila

  AlbumsView        web/src/components/library/AlbumsView.tsx
    - Grid álbumes + búsqueda
    - Detalle inline: carátula con menú ⋮, artista, "Reproducir álbum"
    - Lista tracks abajo con PlayableArtwork + SongActionsMenu

  ArtistsView       web/src/components/library/ArtistsView.tsx
    - Lista artistas → detalle inline con tracklist

  PlaylistsView     web/src/components/playlist/PlaylistsView.tsx
    - CRUD playlists, detalle con canciones, mosaic artwork

  EqualizerView     web/src/components/equalizer/EqualizerView.tsx
    - 10 bandas verticales estilo fader rojo (±12 dB, step 0.5)
    - Presets: Flat, Bass, Treble, Rock, Pop, Jazz, Classical, Electronic, Vocal
    - Toggle activar/desactivar

  SearchView        web/src/components/search/SearchView.tsx
    - Resultados agrupados: canciones, artistas, álbumes

  MiniPlayer        web/src/components/player/MiniPlayer.tsx
    - Barra progreso, artwork, título/artista, tiempo, prev/play/next
    - VolumeFader horizontal (mute + slider rojo)

  FullPlayer        web/src/components/player/FullPlayer.tsx
    - Overlay fullscreen, blur artwork, cola, repeat/shuffle, VolumeFader

--- Menú ⋮ canción (SongActionsMenu) ---

  Archivo: web/src/components/ui/SongActionsMenu.tsx
  Acciones:
    - Agregar a playlist (lista dinámica)
    - Agregar a la cola
    - Quitar de playlist (si contexto playlist)
    - Eliminar canción (confirmación)

--- Stores Zustand ---

  libraryStore   web/src/store/libraryStore.ts
    songs, albums, artists (derivados), scan, deleteSong, artwork

  playerStore    web/src/store/playerStore.ts
    cola, play/pause, seek, volume, mute, repeat, shuffle, full player

  eqStore        web/src/store/eqStore.ts
    10 bandas, presets, enabled

  playlistStore  web/src/store/playlistStore.ts
    CRUD playlists, add/remove songs

--- Servicios ---

  scannerService.ts   — escaneo archivos/carpetas, formatos, inferencia metadata path
  dbService.ts        — Dexie: songs, playlists, songBlobs, songArtworks
  audioService.ts     — HTML audio + cadena BiquadFilter EQ
  artworkService.ts   — tags embebidos + backfill iTunes Search API
                        (fetchArtworkUrl, enrichSongsArtwork, pickBestMatch)
  artworkFileService.ts — cache URLs locales (local://) y resolución src
  audioFileService.ts — cache blobs en memoria

--- Carátulas en la WEB (referencia para la APK) ---

  Flujo en ArtworkDisplay (web/src/components/ui/ArtworkDisplay.tsx):
    1) resolveArtworkSrc() — artwork embebido del escaneo (jsmediatags APIC)
    2) Si no hay imagen → fetchArtworkUrl() vía iTunes Search API
    3) Al encontrar URL → updateSongArtwork() + cache en store/IndexedDB

  artworkService.ts — lógica iTunes:
    - normalizeQuery() limpia paréntesis/corchetes del título
    - isGenericAlbum(): vacío o "unknown album" (NO trata "Music" como genérico)
    - isGenericArtist(): vacío o "unknown artist"
    - Orden de búsqueda fetchArtworkUrl():
        a) Si hay título y álbum genérico → song: "{artist} {title}"
        b) Si álbum conocido → album: "{artist} {album}"
        c) Fallback → song: "{artist} {title}"
    - pickBestMatch(): puntúa artista/álbum/título; si score=0 usa results[0]
    - upgradeArtworkSize(): URLs 100x100 → 600x600

  scannerService.ts — metadata para carátulas:
    - jsmediatags lee APIC/picture del archivo
    - inferMetadataFromPath() + parseFilenameMetadata() desde nombre/ruta
    - Ejemplo: "Fire Force - Opening 1  Inferno.mp3"
        → artista: Fire Force, título: Opening 1 Inferno
    - enrichSongsArtwork() en libraryStore tras cargar/escanear biblioteca

  backfillArtworks() en libraryStore:
    - Recorre canciones sin artwork
    - Comparte carátula por álbum si artista/álbum no son genéricos
    - Delay 120 ms entre peticiones iTunes

--- Formatos soportados (web) ---

  mp3, flac, aac, m4a, ogg, wav, opus, wma, aiff, alac

--- Escaneo biblioteca (web) ---

  - showDirectoryPicker() recursivo (Chrome/Edge desktop)
  - Fallback: input type=file webkitdirectory
  - input múltiple para archivos sueltos
  - Metadatos: jsmediatags + duración vía Audio element
  - Fallback álbum/artista desde nombre/ruta (inferMetadataFromPath)
  - Persistencia IndexedDB; blobs de audio y artwork locales

--- Modelo Song (web/src/types/index.ts) ---

  id, title, artist, album, genre, duration (ms), fileSize, trackNumber,
  format, isLossless, dateAdded, filePath, artwork, file (transient File)

--- Componentes UI compartidos ---

  ArtworkDisplay, AlbumCoverArt, PlayableArtwork, VolumeFader, AppIcon,
  ConfirmDialog, IconButton, songMenuUtils

--- Branding ---

  web/src/constants/appBranding.ts
  APP_NAME: Rugdraiger Play
  APK_DOWNLOAD_URL: {base}apk/rugdraiger-play.apk


================================================================================
B) APP ANDROID NATIVA — Flutter (última versión)
================================================================================

--- Qué es ---

  App NATIVA Flutter (NO WebView, NO Capacitor).
  Replica interfaz y mecánica de la web descrita en sección A.
  Carpeta: rugdraiger_player/
  Package Android: com.rugdraiger.rugdraiger_player
  Nombre visible: Rugdraiger Play

--- APK publicado ---

  Archivo: apk/rugdraiger-play.apk (~25 MB ARM64)
  URL: https://rugdraiger.github.io/rugdraiger_play/apk/rugdraiger-play.apk
  Página: https://rugdraiger.github.io/rugdraiger_play/
  Build: ./scripts/build-apk.sh (Flutter, JDK 17, Android SDK, compileSdk 36)
  Copia web: web/public/apk/rugdraiger-play.apk
  Deploy gh-pages: ./scripts/deploy-pwa.sh (o push manual desde web/dist)
  Último deploy gh-pages: 23 mayo 2026 (commit cbe62d1 — APK con carátulas iTunes)

--- Cambios recientes APK (23 mayo 2026) — RESUMEN ---

  PROBLEMA:
    La canción de prueba "Fire Force - Opening 1  Inferno.mp3" reproducía bien
    pero NO mostraba carátula (placeholder con nota musical).
    MediaStore devolvía artista "<unknown>", álbum "Music", sin APIC válido.
    El MP3 no tiene carátula embebida real (JPEG en binario son falsos positivos).

  SOLUCIÓN (paridad con la web):
    Replicar el flujo web: inferir metadata del nombre de archivo + iTunes fallback.

  ARCHIVOS NUEVOS:
    lib/services/filename_metadata.dart
      - Port de parseFilenameMetadata / inferMetadataFromPath (scannerService web)
      - resolveArtworkMeta() combina tags MediaStore + inferencia del path
      - isGenericArtist(): unknown artist, <unknown>, unknown
      - isGenericAlbum(): unknown album, music (carpeta genérica MediaStore)

    lib/services/artwork_fetcher.dart
      - Port de artworkService.ts (iTunes Search API)
      - fetchForSong() descarga bytes JPEG/PNG desde artworkUrl600/100
      - Misma secuencia de búsqueda que fetchArtworkUrl() en la web
      - Cache en memoria por canción/álbum

    lib/services/embedded_artwork_extractor.dart
      - Extrae APIC de ID3v2 + escaneo JPEG/PNG validado
      - Validación isValidJpeg() evita falsos positivos del stream MP3
      - En Android usa MethodChannel nativo (no pasa el MP3 entero por canal)

    test/artwork_fire_force_test.dart
      - Verifica inferencia "Fire Force" desde nombre de archivo
      - Verifica descarga carátula iTunes para Fire Force Inferno

  ARCHIVOS MODIFICADOS:
    lib/services/artwork_cache.dart
      - loadForSong(): cache → MediaStore queryArtwork → embebido → iTunes
      - enrichAll() equivalente a enrichSongsArtwork() de la web
      - Cache por canción y por álbum (_albumCache)
      - prefetchAll() delega en enrichAll()

    lib/presentation/widgets/artwork_widget.dart
      - Ya NO depende de QueryArtworkWidget (IDs sintéticos fallaban)
      - Image.memory con errorBuilder → reintento remoto
      - Placeholder con CircularProgressIndicator mientras carga
      - LargeArtworkWidget con misma lógica

    lib/data/repositories/music_repository.dart
      - _fromOaqSong(): infiere artista/álbum/título desde filePath si genéricos
      - scanDirectory(): usa inferMetadataFromPath() al indexar carpeta
      - filePath prioriza song.data (ruta filesystem) sobre content:// URI
      - resolveForPlayback() mantiene resolución URI content:// para play

    lib/presentation/bloc/library/library_bloc.dart
      - ClearLibraryEvent + botón "Limpiar biblioteca" (doble confirmación)
      - Tras LoadLibrary → ArtworkCache.prefetchAll() / enrichAll()

    lib/main.dart
      - _PermissionGate: pantalla obligatoria "PERMITIR ACCESO" al abrir
      - JustAudioBackground.init() en main() antes de runApp()
      - autoScanOnStart en MainScaffold si biblioteca vacía

    lib/services/permission_service.dart (nuevo en iteración previa)
      - hasMediaAccess / requestMediaAccess vía on_audio_query + permission_handler
      - READ_MEDIA_AUDIO + READ_MEDIA_IMAGES para queryArtwork

    android/.../MainActivity.kt
      - Extiende AudioServiceActivity (requerido por just_audio_background)
      - MethodChannel "rugdraiger/artwork" → extractArtwork(path)
      - Lee content:// y filesystem nativamente; extrae JPEG/PNG/ID3 APIC

    pubspec.yaml
      - Dependencia explícita: http ^1.2.2 (peticiones iTunes)

  PRUEBA VERIFICADA (emulador Android 34):
    - Archivo: apk/Fire Force - Opening 1  Inferno.mp3
    - Tras escanear: artista "Fire Force", carátula Inferno visible en lista
    - Tests: flutter test test/artwork_fire_force_test.dart → OK

  CANCIÓN DE PRUEBA:
    Local: apk/Fire Force - Opening 1  Inferno.mp3
    Downloads: ~/Downloads/Fire Force - Opening 1  Inferno.mp3
    Nota: NO tiene APIC ID3 válido; carátula viene de iTunes (Mrs. GREEN APPLE / Inferno)

--- Stack tecnológico ---

  Flutter 3.x / Dart >=3.4
  Estado: flutter_bloc (PlayerBloc, LibraryBloc)
  DB: sqflite (SQLite) — rugdraiger.db
  Escaneo: on_audio_query (MediaStore) + file_picker (carpeta específica)
  Audio: just_audio + just_audio_background + audio_session
  EQ UI: EqualizerService (SharedPreferences) — UI estilo web; DSP nativo limitado
  Permisos: permission_handler + on_audio_query (READ_MEDIA_AUDIO, READ_MEDIA_IMAGES)
  HTTP: http (iTunes Search API para carátulas)
  MainActivity: AudioServiceActivity + MethodChannel artwork nativo

--- Carátulas en la APK (paridad con web) ---

  Orden de resolución (ArtworkCache.loadForSong):
    1) Cache memoria (ArtworkCache.get)
    2) song.artwork en memoria (si existiera)
    3) Cache por álbum (_albumCache)
    4) MediaStore queryArtwork (JPEG y PNG, id MediaStore real)
    5) Extracción embebida del archivo (EmbeddedArtworkExtractor + Kotlin nativo)
    6) iTunes Search API (ArtworkFetcher — igual que artworkService.ts web)

  ArtworkWidget / LargeArtworkWidget:
    - Muestra Image.memory cuando hay bytes
    - Spinner mientras carga; placeholder nota musical si falla todo
    - errorBuilder reintenta carga remota

  Metadata para búsqueda iTunes:
    - Si MediaStore dice "<unknown>" / "Music", se infiere del nombre archivo
    - "Fire Force - Opening 1  Inferno.mp3" → artista Fire Force, título con Inferno
    - Sin inferencia, iTunes no encuentra carátula (término "<unknown> ..." falla)

  Limitaciones conocidas:
    - Carátulas iTunes requieren INTERNET en el dispositivo
    - No se persisten bytes de carátula en SQLite (solo cache sesión en memoria)
    - MP3 sin APIC y sin match iTunes → placeholder
    - queryArtwork MediaStore suele fallar si no hay arte indexado en el sistema

--- Identidad visual (lib/core/theme/app_colors.dart) ---

  Mismos tokens que la web:
  background #0A0A0A, surface #141414, surfaceElevated #1C1C1E
  accent #FF2020, textSecondary #8E8E93, textTertiary #48484A
  Icono app: assets/icons/app_icon.png (copiado de web icon-512)
  Launcher Android: mipmaps generados en build-apk.sh desde app_icon.png

--- Arquitectura navegación ---

  MainScaffold (lib/presentation/screens/home/main_scaffold.dart)
  ViewName enum (lib/core/navigation/view_name.dart):
    home, library, songs, albums, artists, playlists, equalizer, search

  BottomNavBar (5 + Más): Inicio | Canciones | Playlists | Ecualizador | Buscar | Más
  MobileNavDrawer: las 8 vistas (sin botón Download — es app nativa)
  MiniPlayer fijo sobre bottom nav
  FullPlayer: PlayerScreen modal slide-up

--- Pantallas Flutter ---

  HomeScreen           lib/presentation/screens/home/home_screen.dart
    - AppIconWidget + stats
    - "Escanear biblioteca" (MediaStore completo)
    - "Elegir carpeta" (file_picker getDirectoryPath → scanDirectory)
    - "Limpiar biblioteca" (doble confirmación, ClearLibraryEvent)
    - Recientes, preview álbumes

  LibraryHubScreen     lib/presentation/screens/library/library_hub_screen.dart
    - 4 tarjetas como LibraryView web

  SongsScreen          lib/presentation/screens/library/songs_screen.dart
    - Búsqueda, Reproducir, lista con formato, menú ⋮

  AlbumsScreen         lib/presentation/screens/library/albums_screen.dart
    - Grid + búsqueda
    - Detalle: carátula con ⋮, artista, "Reproducir álbum", lista tracks con ⋮

  ArtistsScreen        lib/presentation/screens/library/artists_screen.dart
    - Lista → detalle con tracklist y ⋮

  PlaylistsScreen      lib/presentation/screens/playlist/playlists_screen.dart
    - Crear playlist, detalle con canciones, ⋮ con playlistId

  EqualizerScreen      lib/presentation/screens/equalizer/equalizer_screen.dart
    - Faders verticales EqBandFader (estilo web)
    - Presets, toggle, Restablecer

  SearchScreen         lib/presentation/screens/search/search_screen.dart

  PlayerScreen         lib/presentation/screens/player/player_screen.dart
    - Artwork, controles, cola, VolumeFader

  MiniPlayer           lib/presentation/widgets/mini_player.dart
    - Progreso, artwork, tiempo, controles, VolumeFader (width 80)

--- Menú ⋮ canción ---

  Archivo: lib/presentation/widgets/song_actions_sheet.dart
  Acciones:
    - Agregar a playlist
    - Agregar a la cola
    - Quitar de playlist (si aplica)
    - Eliminar canción (confirmación)

--- BLoC / eventos principales ---

  LibraryBloc:
    LoadLibrary, ScanLibrary, ScanFolder(path), SearchSongs,
    CreatePlaylist, AddSongToPlaylist, DeletePlaylist,
    DeleteSong, RemoveSongFromPlaylist, ClearLibrary

  PlayerBloc:
    PlaySong, Pause, TogglePlayPause, Seek, SkipNext/Prev,
    SetVolume, ToggleMute, ToggleShuffle, SetRepeatMode, AddToQueue

--- Servicios ---

  audio_service.dart           — just_audio, URIs file/content, cola, volumen, mute
  music_repository.dart        — escaneo MediaStore + carpeta, inferencia metadata path
  database_helper.dart         — songs, playlists, playlist_songs, recently_played
  equalizer_service.dart       — presets/bandas en SharedPreferences
  permission_service.dart      — permisos media al abrir (obligatorio)
  artwork_cache.dart           — cache carátulas + enrichAll tras cargar biblioteca
  artwork_fetcher.dart         — iTunes Search API (port web artworkService.ts)
  embedded_artwork_extractor.dart — ID3 APIC + JPEG/PNG embebido validado
  filename_metadata.dart       — inferencia artista/álbum/título desde path (port web)

--- Reproducción audio (Flutter) ---

  - MainActivity extiende AudioServiceActivity (obligatorio just_audio_background)
  - JustAudioBackground.init() en main() ANTES de runApp()
  - Rutas: Uri.file() o Uri.parse(content://...)
  - resolveForPlayback() re-resuelve URI content:// fresco desde MediaStore
  - ConcatenatingAudioSource + MediaItem (just_audio_background)
  - Fallback playlist/URI individual si falla background tag
  - audio_session.setActive(true) antes de reproducir
  - Notificación Android foreground al reproducir
  - Errores visibles en mini player (errorMessage)

--- Permisos al abrir (Flutter) ---

  - Pantalla _PermissionGate en main.dart (no se puede omitir)
  - Botón "PERMITIR ACCESO" → PermissionService.requestMediaAccess()
  - Si denegado permanente → "ABRIR AJUSTES"
  - Tras conceder → MainScaffold(autoScanOnStart: true) si biblioteca vacía
  - AndroidManifest: READ_MEDIA_AUDIO, READ_MEDIA_IMAGES, READ_EXTERNAL_STORAGE (≤32)

--- Escaneo biblioteca (Flutter) ---

  1) ScanLibraryEvent → on_audio_query querySongs (todo el dispositivo)
     + inferencia metadata desde filePath (_fromOaqSong)
  2) ScanFolderEvent → file_picker carpeta → scanDirectory recursivo
     + inferencia metadata desde path
  3) Tras cargar → ArtworkCache.enrichAll() precarga carátulas (MediaStore + iTunes)
  Formatos: mp3, flac, aac, wav, ogg, m4a, alac, aiff, opus, wma

--- Formatos / permisos Android ---

  AndroidManifest: READ_MEDIA_AUDIO, READ_MEDIA_IMAGES, READ_EXTERNAL_STORAGE (≤32),
  INTERNET, WAKE_LOCK, FOREGROUND_SERVICE, FOREGROUND_SERVICE_MEDIA_PLAYBACK,
  POST_NOTIFICATIONS
  compileSdk: 36 (app + subprojects en android/build.gradle.kts)

--- Widgets UI clave ---

  AppIconWidget, VolumeFader, EqBandFader, SongActionsSheet,
  ArtworkWidget, LargeArtworkWidget (Image.memory + iTunes fallback),
  AlbumCard, SongListTile, BottomNavBar, MobileNavDrawer

  NOTA: ArtworkWidget ya NO usa QueryArtworkWidget como fallback principal.
  QueryArtworkWidget fallaba con IDs sintéticos de scanDirectory y sin arte MediaStore.

--- Archivos obsoletos / no usar para APK ---

  rugdraiger_player/lib/presentation/screens/library/library_screen.dart (tabs viejo)
  rugdraiger_player/lib/presentation/screens/user/user_screen.dart (no en nav)
  web/android/ (Capacitor — no genera el APK publicado)

--- Comandos útiles ---

  Analizar Flutter:  cd rugdraiger_player && flutter analyze
  Test carátulas:    cd rugdraiger_player && flutter test test/artwork_fire_force_test.dart
  APK release:       ./scripts/build-apk.sh
  Publicar web+APK:  ./scripts/deploy-pwa.sh
                     (push gh-pages desde web/dist con apk/rugdraiger-play.apk)

--- Paridad web ↔ Flutter (estado actual) ---

  IMPLEMENTADO en Flutter:
    - Navegación y layout móvil equivalente
    - Colores y tipografía alineados
    - Escaneo dispositivo + carpeta específica
    - Menú ⋮ canción y detalle álbum con lista
    - VolumeFader en mini/full player
    - EQ faders verticales UI
    - Icono app real
    - Reproducción nativa con just_audio + AudioServiceActivity
    - Permisos obligatorios al abrir
    - Limpiar biblioteca (HomeScreen)
    - Inferencia metadata desde nombre/ruta de archivo (port web scannerService)
    - Carátulas: embebidas + iTunes + MediaStore (solo álbum real, port web artworkService)
    - Carga carátulas bajo demanda en widgets (sin prefetch masivo al escanear)
    - Reproductor full: sin menú ⋮ ni barra inferior (LIKE/LYRICS/QUEUE/OUTPUT)
    - Botón corazón funcional → sección Me gusta en Inicio + pantalla Favoritos
    - Sección Lo más escuchado (top 4 por play_count) en Inicio
    - Mini player sin barra de volumen
    - Reproductor: menú ⋮ con «Buscar imagen de carátula» (iTunes + persistencia en disco)
    - Layout responsivo en reproductor (compacto para pantallas ~720×1600)

  DIFERENCIAS / pendientes vs web:
    - Web: File System Access API / picker navegador; Flutter: MediaStore + file_picker
    - Web: EQ procesa audio (Web Audio API); Flutter: EQ guarda prefs, no DSP nativo completo
    - Web: artwork persistido en IndexedDB (URLs/data); Flutter: cache solo en memoria
    - Web: jsmediatags APIC en cliente; Flutter: ID3 nativo + iTunes si no hay APIC
    - Web: Dexie blobs por canción; Flutter: paths URI del sistema
    - Web: sidebar desktop; Flutter: solo layout móvil
    - Web: PWA offline; Flutter: app instalada APK
    - Carátulas iTunes en APK requieren conexión a internet

--- Bugs corregidos (historial relevante) ---

  | Problema                         | Causa                          | Fix                          |
  |----------------------------------|--------------------------------|------------------------------|
  | No reproduce                     | MainActivity + init audio      | AudioServiceActivity + main()|
  | Eliminar canción no funciona     | context tras Navigator.pop     | rootContext en song_actions  |
  | Permisos insuficientes           | Faltaba READ_MEDIA_IMAGES      | Manifest + PermissionService |
  | Sin carátula Fire Force Inferno  | Metadata <unknown> + sin APIC| filename_metadata + iTunes   |
  | JPEG falso en MP3                | Stream MP3 parece JPEG         | isValidJpeg() + iTunes       |
  | MethodChannel fallaba con MP3    | APK 2MB excede límite canal    | extractArtwork nativo Kotlin |
  | Carátulas clonadas (4 canciones) | MediaStore mismo álbum "Music" | iTunes por canción + cache songId |
  | ANR al abrir lista Canciones     | querySongs + prefetch paralelo | cola serial + sin prefetch + skip query |
  | Álbum home muestra "0"           | MediaStore album id numérico   | isGenericAlbum + fallback artista |

--- Objetivo para otra IA ---

  La WEB es la referencia de diseño, UX y funcionalidades.
  La app FLUTTER debe seguir evolucionando hacia paridad 1:1 con la web,
  manteniendo ventajas nativas (MediaStore, background playback, notificaciones).
  NO reemplazar Flutter por Capacitor/WebView.


================================================================================
FIN DEL DOCUMENTO
================================================================================
